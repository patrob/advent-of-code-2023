import DayX from "./dayX";

describe("Day X", () => {
    let dayX: DayX;
    beforeEach(() => {
        dayX = new DayX();
    });

    it("should create", () => {
        expect(dayX).toBeTruthy();
    });
});
